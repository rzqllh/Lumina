# Cloudflare Workers Static Assets Configuration
# Lumina uses Cloudflare strictly for static asset hosting (SPA delivery).
# All backend / privileged operations reside in Supabase Edge Functions.

name = "lumina"
compatibility_date = "2024-11-01"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
