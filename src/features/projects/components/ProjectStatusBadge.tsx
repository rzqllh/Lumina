/**
 * ProjectStatusBadge — delegate to canonical G-005 StatusBadge.
 * All project status styling uses OKLCH semantic tokens, not raw Tailwind color classes.
 */
export { ProjectStatusBadge } from '@/components/ui/status-badge';
